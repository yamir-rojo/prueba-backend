"ayudame a generar los commits, me tira path not found"

"calcula además el precio con descuento del 20% (redondeado a entero) y una etiqueta decidida con
if / elif / else a partir de multijugador y oferta : "Para jugar en grupo" si es multijugador y no está en
oferta, "En oferta" si está en oferta, "Aventura individual" --no recordaba.

"para generar el HTML y el CSS de la interfaz utiliza estos ejemplos" enviadas imagenes de profesor.

"css/estilos.css con estilos propios visibles (fondo, tarjetas o lista, botón o enlace destacado)."

"Una imagen (logo, ícono o foto) cargada con {% static %} . añade esta imagen gato.jpg"

"ordename el commit porque los hice sin orden"

"README.md debe explicar cómo ejecutarlo (crear el
entorno, instalar requirements.txt , migrate , runserver )."


"https://github.com/yamir-rojo/prueba-backend.git ese es mi git

revisa qué me falta"

"sube el ultimo commit"

"haz otro commit solo corriguiendo el ia.txt que se llame "fix ia.txt""